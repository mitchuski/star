{
  "name": "swordsmans-key",
  "version": "0.1.0",
  "private": true,
  "description": "The Swordsman's Key — the walkable model (/star + /lattice). Static site; npm run dev serves the repo root.",
  "scripts": {
    "dev": "serve . -l 8000",
    "start": "serve . -l 8000",
    "build": "node scripts/verify-static.cjs"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
