# Star carry and first contact: VTA + Star knowledge spaces

8 September 2026 · integration design, not a live-service claim.

Canonical contract: [The agent knowledge space](../../cityofmages/mages-city/KNOWLEDGE_SPACES.md). Shared backlog: [KS-01–KS-07](../../cityofmages/mages-city/KNOWLEDGE_SPACE_TASKS.json).

Own KS-02 and the display half of KS-05. The browser houses the continuing key; each participating site receives an allowed projection automatically through the existing bridge design. Preserve the City Key v1 carrier and unknown fields. The Star is the transport/presentation surface for credentials; receiving services verify and enforce them.

Reuse the current dual-agent geometry and the 64-vertex encoding. Align with PLAN_STAR_CORE_ARTEFACT.md: SHAPE(key) remains distinct from a time/context-dependent VIEW. Core artefacts stay in private custody unless explicitly released. Do not interpret elapsed animation time as evidence or rewrite κ when only the view changes.

First contact reveals intent and context without making key-loaded equal login. Show identity, delegation, context grant, expiry and refusal separately. Same-origin BroadcastChannel is not the cross-origin transport. Gate: two allowed origins, disallowed-field exclusion, stable unchanged export and explicit evolution after an authorized contribution.
