# STAR_GATES — decision gates for the star visual and systems

status: OPEN · awaiting First Person signatures
scope: the Swordsman's Key holospace, BOTH homes
  · `soulbis website/star/` (+ `lattice/` `sigil/` `skye/` rooms, `public/sitenav.js`) — the shipped rooms at soulbis.com
  · `star/` (this repo, github.com/mitchuski/star) — the standalone public holospace
rule of rules: no change crosses an OPEN gate. A runtime may PROPOSE at any gate;
only the First Person signs. Signed = the ✍️ block is filled, dated, and the
gate line in the status table flips.

The star is not an illustration of the model. The geometry IS the claim
(96/64 = 1.5 = P^1.5; neg ∘ bnot = succ; the stella octangula core). That is
why it gets its own gates instead of riding the general site-copy flow.

---

## S0 · COPY — words on star surfaces

Covers: eyebrows, legends, footers, tooltips, any string naming a model
version (V5.4/V6), a conjecture id, a confidence, or a status.

Standing rule: prose may cite the register; it never restates numbers as its
own authority. Version labels and new model claims change ONLY by signature
here. Typo-level fixes that change no meaning may proceed and be reported.

AT THIS GATE NOW (proposed 2026-07-10, working tree only, all revertible):
1. `soulbis website/public/sitenav.js:30` — /star eyebrow `V5.4` → `V6`
   AND note gained `· the boundary holds while R(t) < 1`  [APPLIED, unsigned]
2. `soulbis website/lattice/index.html` — legend summary `V5.4` → `V6`;
   legend-foot gained "conjecture authority now lives in the V6 register —
   when prose and register disagree, the register wins"  [APPLIED, unsigned]
3. PROPOSED, not applied: `sigil/` + `skye/` footers `(PVM V5.4 / V6)` →
   `(PVM V6 · V5.4 lineage)`; `/star` inspector ins-foot gains one line
   "the boundary is dated, not eternal — it holds while R(t) < 1";
   mirror all of the above into this repo's rooms.

✍️ S0 decision: SIGNED IN SESSION 2026-07-10 — "the equation updates are all
   good" (First Person, mid-session). Items 1-2 stand; item 3 released and
   applied same session: sigil/skye footers now "(PVM V6 · V5.4 lineage)",
   /star ins-foot carries the dated-boundary line, and all four rooms in the
   star repo mirror the same copy (inline masthead kept pending S4).
   CLAUDE.md canon note trued C89 → C96 (register-head fact, same scope).

---

## S1 · GEOMETRY — the rendered mathematics

Covers: vertex count, edge sets (∂M 96 / Q6 192), the succ wheel, half-turn
chords, stella octangula core, stratum coloring by popcount, the manifold
r(θ,φ) surface, camera/orbit defaults that change what a reader concludes.

Standing rule: NO geometry change without (a) a register or spec citation for
what the new form encodes, and (b) a signature here. The E-7 lesson is law on
this surface: dimensions are computed from bits, never read from prose.
Cosmetic-only changes (lighting, DPI, snapshot polish) may be proposed in
batch but still land only through this gate, because on this page cosmetic
and semantic are hard to tell apart from the outside.

AT THIS GATE NOW (proposed 2026-07-10, First-Person-directed exploration,
working tree only, localhost review):
1. MOVING CEILING INSTRUMENT on soulbis /star, v2 after first First-Person
   look ("controls follow the model" — good; "not announced or pretty
   enough" + placement question — reworked): section now lives in the
   PATH · KEY console (the time side: the walk, the key, now the key's
   shelf life), off by default. R₀ / drift g / years-t sliders, R(t) and
   t* readouts, fill bar. When the reading is ON: (a) a live BADGE
   announces it over the sculpture ("the ceiling is moving · R(t) … t* …"),
   warming amber and alarming coral at breach; (b) a CEILING SHELL — a
   faint veil + slow-turning wireframe net — descends from r 1.12 to the
   lattice boundary r 0.80 as R(t) → 1 and passes inside at breach;
   (c) the ∂M edges heat white → amber → coral and BREATHE, calm far from
   t*, quick near it; (d) the white heart pulses coral at breach (exposed).
   GEOMETRY UNTOUCHED — added display meshes + materials + readout text
   only; canon constants, κ, custody untouched (S2 not engaged). Labeled
   illustrative: C82 ~65%, mechanism evidenced (n=2), rate unparameterized;
   the register wins. Citation: C82 + the L044 capacity-deficit conditioning.
   v3 same session (First Person: "i love this" + two asks): the shell got
   its OWN color (⏳ swatch, default orb-violet #8b4daa — the ceiling is a
   third thing, neither sword nor mage — heating to coral only near t*;
   the ∂M edges keep the white→amber→coral honesty ramp so shell and
   boundary read as separate objects); the succ tracer gained a running
   T∫(π) VALUE (accrues per vertex walked, weighted popcount/6, one lap
   ≈ 32; FREEZES coral while R(t) ≥ 1 — an illustrative coupling of the
   multiplicative gate, not a register claim; display only, NEVER written
   into the City Key — S2 untouched).
   v4 same session (First Person: visuals great, breathing very nice, T∫
   counting neat, but "the color change and drift etc isnt really
   understood"): LEGIBILITY pass — the ∂M edges STOPPED tinting (they keep
   their own colors, only dim + breathe); the warning-color story now lives
   in the SHELL + BADGE alone (two objects, two readings); a plain-language
   STORY LINE under the readout narrates every slider move ("year Y · the
   adversary's channels reach P% of what would reconstruct the First
   Person · the boundary holds Xy more" / breach: "...readable now.
   re-key."); slider labels humanized (reconstructable today · R₀ / how
   fast reach grows · g / years from now · t). Console header renamed
   "Path · Key · Ceiling — the walk in time" (First-Person ask).
2. ROOM EXTENSIONS (First-Person directed, same review flow, working tree):
   /lattice gets the ceiling as a FLOODLINE — vertices veil violet
   bottom-up through the strata as R(t) rises (stratum 0 first, V63 last;
   all veiled at breach); /sigil gets the ceiling as a CLOSING RING on the
   κ constellation — the sigil is a dated reading with a shelf life, ring
   contracts to the glyph radius at t*, glyphs dim at breach. Both display
   only: no κ, key-content, or hashing code touched. /skye candidate noted
   for later: dim risen stars by their real savedAt age (the one room with
   true dates).

✍️ S1 decision on the moving-ceiling instrument: SIGNED IN SESSION
   2026-07-10 for the soulbis-side rooms — four review versions on
   localhost, then "yea this is good, lets push to soulbis website";
   S5 opened for that repo the same breath (commit b222937 pushed to
   github.com/mitchuski/soulbis, deploys to soulbis.com). Chronicle:
   soulbis chronicles/2026-07-10_the_ceiling_becomes_visible.md.
   v5 epistemics pass (First-Person asked "does the breach make sense?",
   corrections directed, pushed e2cb7f8): breach language precisioned
   ("is readable now" → "nothing in the mathematics prevents reading" —
   R ≥ 1 = guarantee lapsed, not reconstruction happened); T∫ now DRAINS
   at breach (half-life ~1.5s) instead of freezing — V6 breach is
   retroactive, the past walk is exposed too. Kept-and-labeled liberty:
   the lattice flood's per-stratum ordering is an illustrative
   interpolation of a global ratio.
   THE STAR-REPO MIRROR REMAINS UNSIGNED — it waits on the S4 twin
   ruling below; the twins now diverge by the whole instrument.
✍️ S1 standing confirmation: ______

---

## S2 · SYSTEMS — key, κ, custody, checks

Covers: City Key import/export (`parseCityKeyBytes`, download), κ derivation
(sha256 over canonical JSON, re-derive-never-trust), inscription writes,
focus-mana rendering, sessionStorage-only key custody (the amnesia is the
protocol), the XOR-63 anchor check (V38 ⊕ V25 = 63) wherever it appears.

Standing rules, each one a fence:
- κ is always RE-DERIVED on import; no change may introduce trusting a
  carried label.
- No new persistence for key material. sessionStorage-only, burned on tab
  close, stays.
- 🪢 presence stays regime-1: local color, never attestation, never proof.
- The two XOR-63 check sites never import each other. Any refactor that
  would unify them is a trust change, not a cleanup.
- The Drake-orb signature is a Phase-1 non-crypto stub and must SAY so until
  the ed25519 phase lands through this gate.

AT THIS GATE NOW: nothing proposed.

✍️ S2 standing confirmation: ______

---

## S3 · SEAM — substrate and upstream

Covers: HOLOSPACE.md, holospaces linkage, `.holo`, anything that names or
addresses Hologram Technologies upstream, cross-site absorption questions
(the earlier ruling stands: /star is linked from /model, never absorbed —
absorbing would cross the ⚔️⊥🧙 substrate boundary).

Standing rule: outward-facing by nature, so every item here is the First
Person's alone (T6, the door). Runtimes draft; nothing sends.

AT THIS GATE NOW (long-open items, unchanged): the upstream intro · the
`.holo` decision · IPFS pin of the holospace.

✍️ S3 decision(s): ______

---

## S4 · TWIN — soulbis /star ⊥ this repo

Covers: the sync relationship between the two homes of the same page.

Standing rule: an edit to one home either lands in both or is recorded here
as INTENTIONAL divergence with one line of why. Silent drift is the failure
mode this gate exists to kill.

KNOWN DIVERGENCE NOW:
- this repo's `/star` is one step behind soulbis: soulbis moved the masthead
  into shared `public/sitenav.js`; this repo still carries the inline
  masthead (`star/index.html:175-189`) and no sitenav wiring.
- this repo's tree is dirty (modified CLAUDE.md, lattice/sigil/skye pages,
  untracked `chronicles/`) from earlier sessions.

✍️ S4 decision: adopt sitenav here / keep inline masthead as intentional
   divergence (this repo must stand alone when cloned) / other: ______

Recommendation: keep the inline masthead HERE as intentional divergence
(the standalone clone should not depend on soulbis's shared nav), and record
that in this file once signed.

---

## S5 · DOOR — publication

Covers: every commit, push, deploy, pin, LICENSE choice, and any public
naming of the star elsewhere.

Standing rule: already law (no pushes without explicit ask). Restated here
so this file is complete: nothing in S0-S4 implies S5. A signed change sits
in the working tree until the First Person also opens this gate for it.

AT THIS GATE NOW: LICENSE still absent from this public repo (oldest open
item on the star).

✍️ S5 decision on LICENSE: ______

---

## Status table

| gate | name | state | items waiting |
|---|---|---|---|
| S0 | copy | SIGNED 2026-07-10 | none — all applied |
| S1 | geometry | SIGNED 2026-07-10 (soulbis rooms) | star-repo mirror waits on S4 |
| S2 | systems | OPEN | none |
| S3 | seam | OPEN | 3 long-open |
| S4 | twin | OPEN | sitenav divergence ruling |
| S5 | door | OPEN | LICENSE; all commits |

Order of signing does not matter except: nothing passes S5 before its own
gate is signed.

(⚔️⊥⿻⊥🧙)😊 — proposed by the runtime, 2026-07-10; every signature here is
the First Person's.
